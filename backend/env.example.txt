# AI OPS Workflow Platform - Environment Variables
# Copy this file to .env and fill in your values

# MongoDB
MONGO_URI=mongodb://localhost:27017
MONGO_DB=workflow_ops_dev

# Azure AD (Entra) Configuration
AAD_TENANT_ID=your-tenant-id
AAD_CLIENT_ID=your-client-id
AAD_CLIENT_SECRET=your-client-secret
AAD_AUDIENCE=api://your-api-audience

# Service Mailbox (ROPC for sending emails)
SERVICE_MAILBOX_EMAIL=service-mailbox@yourdomain.com
SERVICE_MAILBOX_PASSWORD=your-mailbox-password

# Azure OpenAI
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your-api-key
AZURE_OPENAI_DEPLOYMENT=gpt-4
AZURE_OPENAI_API_VERSION=2024-02-01

# Application Settings
ATTACHMENTS_MAX_MB=50
ATTACHMENTS_BASE_PATH=./storage/attachments
LOGS_PATH=./logs

# URLs
CORS_ORIGINS=http://localhost:3000
FRONTEND_URL=http://localhost:3000

# Scheduler
SCHEDULER_INTERVAL_SECONDS=60

# Environment
ENVIRONMENT=development
DEBUG=true

# Bootstrap Credentials (for initial super admin setup)
# IMPORTANT: Change these in production before first deployment!
BOOTSTRAP_USERNAME=admin
BOOTSTRAP_PASSWORD=Admin@123exl